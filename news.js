const p=JSON.parse(localStorage.getItem('careerPlayer')||'null');const box=document.getElementById('newsList');if(!p){box.innerHTML='<div class="stat-card">ابتدا بازیکن بسازید تا اخبار Career شما نمایش داده شود.</div>';}else{const news=[];if((p.goals||0)>0)news.push(`⚽ ${p.name} در این فصل ${p.goals} گل به ثمر رسانده است.`);if((p.assists||0)>0)news.push(`🎯 ${p.name} با ${p.assists} پاس گل، نقش مهمی در حملات تیم دارد.`);if((p.overall||0)>=80)news.push(`⭐ عملکرد درخشان ${p.name} باعث افزایش اعتبار او شده است.`);news.push(`🏟️ ${p.name} در ${p.startingClub||'باشگاه خود'} به فعالیت ادامه می‌دهد.`);news.push(`📈 OVR فعلی بازیکن ${p.overall||0} است.`);if((p.fanRating||50)>=75)news.push(`❤️ هواداران از عملکرد ${p.name} بسیار راضی هستند.`);if((p.fatigue||0)>=70)news.push(`⚠️ کادر فنی درباره خستگی ${p.name} هشدار داده است.`);news.forEach((text,i)=>{const card=document.createElement('div');card.className='stat-card';card.innerHTML=`<small>خبر ${i+1} • فصل ${p.season||'2026/27'}</small><strong>${text}</strong>`;box.appendChild(card);});}