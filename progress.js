const p=JSON.parse(localStorage.getItem('careerPlayer')||'null');const labels={pace:'سرعت',shooting:'شوت',passing:'پاس',dribbling:'دریبل',defending:'دفاع',physical:'قدرت بدنی',mental:'ذهنی',weakfoot:'پای ضعیف'};const playerBox=document.getElementById('progressPlayer'),box=document.getElementById('attributes');if(!p){playerBox.textContent='ابتدا بازیکن بسازید.';}else{playerBox.innerHTML=`<strong>${p.name}</strong><br>OVR: ${p.overall} | سن: ${p.age||'—'} | پتانسیل: ${p.potential||'—'}`;Object.entries(p.attributes||{}).forEach(([key,val])=>{const card=document.createElement('div');card.className='stat-card';card.innerHTML=`<small>${labels[key]||key}</small><strong>${Math.round(val)}</strong><div class="progress"><div class="progress-bar" style="width:${Math.max(0,Math.min(100,val))}%"></div></div>`;box.appendChild(card);});}